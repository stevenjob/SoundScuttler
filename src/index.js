const React = require('react');
const ReactDOM = require('react-dom');

const rootElement = document.getElementById('root');

ReactDOM.render(
  <div>
    thing qwertyuio thing
  </div>,
  rootElement
);
